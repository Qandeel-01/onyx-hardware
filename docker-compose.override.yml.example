version: '3.9'
# Local override file - customize per machine
# Copy to docker-compose.override.yml and edit as needed

services:
  backend:
    # Uncomment to use GPU (NVIDIA)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    environment:
      # Uncomment for debug logging
      # PYTHONDEBUG: 1

  frontend:
    # Adjust port if needed
    ports:
      - "5173:5173"

  db:
    # Map additional ports for external tools
    ports:
      - "5432:5432"

# Override specific services or add new ones below
