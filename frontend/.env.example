# Copy to .env.local. Typical stack: `docker compose up` (db + api only), then `npm run dev` here.
# WebSocket shot stream (FastAPI + Postgres)
VITE_ENABLE_WS=true

# Optional: override WebSocket origin (default: same host as the app — Vite proxies /ws → :8000)
# VITE_WS_URL=ws://127.0.0.1:8000
